---
interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<a
  class="rounded-md bg-blue-600/20 px-1 text-blue-600 dark:bg-blue-500/20 dark:text-blue-500"
  href=`/tags/${tag}/`
>
  #{tag}
</a>
